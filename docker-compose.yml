version:  '3'
services: 
  web:
    build: 
      context: .
      dockerfile: 'Dockerfile.dev'
    volumes: 
      - app/node_modules
      - .:/app
    ports: 
      - 3000:3000
  prod-web:
    build: 
      context: .
      dockerfile: 'Dockerfile.prod'
    ports: 
      - 80:80 
  